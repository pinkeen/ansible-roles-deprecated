<!DOCTYPE html>
<html>
    <head>
        <title>{{ site_host }}</title>
        <style>
            body {
                background: #272822;
                color: #fff;
                font-family: monospace;
                margin: 5%;
            }

            strong {
                color: #499E2C;
            }

            h1 {
                color: #DE2668;
            }

            em {
                font-style: normal;
                color: #60C7C8;
            }

            a {
                color: #9D77DF;
            }
        </style>
    </head>
    <body>
        <h1>Hello, the site <strong>{{ site_host }}</strong> is working!</h1>
        <h2>Ansible config</h2>
        <p>
            <em>site_log_dir</em>: {{ site_log_dir }}<br/>
            <em>site_web_dir</em>: {{ site_web_dir }}<br/>
        </p>
        <small>Hush, the magic string is: 11fc14167367c5ff567438a1624a3956.</small>
    </body>
</html>
